# frozen_string_literal: true

source "https://rubygems.org"

# Add the Jekyll static site generator gem
gem "jekyll"

# Add the theme gem
gem "minima"

# Add any plugin gems
gem "jekyll-feed"

# This is a group for plugins that are useful during development
group :jekyll_plugins do
  # Add any other plugins here
end
